<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".util.MyApps"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.TestAssessment"
        tools:targetApi="31">
        <activity
            android:name=".screen.movie.favorite.ActivityListFavMovie"
            android:exported="false" />
        <activity
            android:name=".screen.movie.detail.ActivityDetailMovies"
            android:exported="false" />
        <activity
            android:name=".screen.movie.ActivityListMovie"
            android:exported="false" />
        <activity
            android:name=".screen.genre.ActivityListGenre"
            android:exported="false" />
        <activity
            android:name=".screen.home.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>